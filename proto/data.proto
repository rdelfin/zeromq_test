syntax = "proto3";

package zeromq_test.data;

// An action message
message Action {
    double angle = 1;
    double speed = 2;
}

// An image
message Image {
    uint64 timestamp = 1;
    int32 width = 2;
    int32 height = 3;
    int32 channels = 4;
    bytes data = 5;
}
